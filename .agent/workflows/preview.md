---
description: Start a local server to preview HTML files
---
1. Start a python HTTP server in the current directory on port 8080. This command will run in the background.
// turbo
2. python -m http.server 8080
